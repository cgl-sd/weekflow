# Weekflow 隐私说明

更新日期：2026-07-27

Weekflow 是本地优先的个人执行应用。正式版的目标、任务、规划、频道、日历事件、专注记录和总结保存在当前用户的 macOS 沙盒 Application Support 中；开发版仅使用项目内 `01_workspace/.data`，两者不会互相读取。

Weekflow 不包含账户、广告、分析统计或遥测 SDK，也不会自动上传业务数据。只有在用户点击“检查更新”时，应用才会向 GitHub 的 Weekflow Releases 页面发送标准 HTTPS 请求；请求不包含本地任务或计划。只有在用户主动选择“导入”“导出”或“导出诊断”时，应用才访问用户在系统文件面板中明确选择的文件。

诊断支持文件只包含应用版本、macOS 版本、数据库记录计数、文件大小、备份状态和错误标记元数据，不包含任务标题、描述、评论、总结正文或本机绝对路径。完整数据归档则包含用户的全部 Weekflow 业务数据，应由用户按敏感文件妥善保管。

应用可在用户主动启用时注册全局日期快捷键；专注计时结束通知需要 macOS 通知授权。MVP 不接入系统日历、提醒事项、iCloud 或 AI API。

卸载应用不会自动删除用户数据。需要彻底删除时，请先在应用内导出所需归档，再退出应用，并删除 macOS 为 `com.weekflow.app` 保存的应用容器。该操作不可恢复，应由用户自行确认。
