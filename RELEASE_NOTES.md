# Weekflow 0.8.0

- 隔离 Debug 项目数据与正式版沙盒数据，开发构建不再读取正式版数据库。
- 增加经 SQLite 完整性校验的滚动备份、备份状态和应用内安全恢复。
- 增加完整数据归档的导入与导出；导入前自动备份并以单事务替换数据。
- 增加不含任务正文和本机路径的诊断支持文件。
- 修复异常退出后任务计时误计离线时长、同步超时后延迟写入、滚动条漂移及历史记录无界增长问题。
- 更新检查支持 SemVer 预发布版本排序，并继续限定为受信任的 GitHub Weekflow Releases 地址。
- 本地发布流程增加应用与 DMG 的 Developer ID 签名、公证、装订、数据洁净检查、SHA-256 校验和与构建清单。
